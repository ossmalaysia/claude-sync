## What changed

## How it was tested

- [ ] `gofmt -l .` prints nothing
- [ ] `go vet ./...` and `go test -race ./...` pass
- [ ] `cd frontend && npm test` passes
- [ ] No real account data (project names, emails, org ids) in code, tests or logs
